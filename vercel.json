{
  "version": 2,
  "buildCommand": "cd ai-chat-hub && npm install --force && npm run build",
  "outputDirectory": "ai-chat-hub/dist",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
